name: auth_system
description: A full authentication and authorization system with Relic and MongoDB
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  relic: ^1.0.0
  mongo_dart: ^0.10.0
  bcrypt: ^1.1.3
  dart_jsonwebtoken: ^2.14.0
  dotenv: ^4.2.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
